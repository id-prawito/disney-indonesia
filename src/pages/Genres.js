import React from "react";
const Genres = () => {
    // const { category, genresnya } = useParams();
    // console.log(category);
    return (
        <div>
            <h1>Hellow</h1>
        </div>
    );
};

export default Genres;
